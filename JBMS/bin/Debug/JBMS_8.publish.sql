/*
Deployment script for JBMS

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "JBMS"
:setvar DefaultFilePrefix "JBMS"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
USE [$(DatabaseName)];


GO
PRINT N'Altering [dbo].[JBMSPolling]...';


GO
ALTER TABLE [dbo].[JBMSPolling] ALTER COLUMN [MessageID] VARCHAR (50) NULL;


GO
PRINT N'Altering [dbo].[Polling]...';


GO
ALTER PROCEDURE [dbo].[Polling]
	@Submitter varchar(50)
AS

	declare @JBMSPollingID int	
	select	@JBMSPollingID = JBMSPollingID 
	from	JBMSPolling
	where	Submitter = @Submitter

	if @JBMSPollingID is null
	begin
		insert into JBMSPolling
			(Submitter, StatusID, LastUpdatedDateTime)
		values
			(@Submitter, 2, getdate())
		set @JBMSPollingID = @@IDENTITY
	end
	
	RETURN @JBMSPollingID
GO
PRINT N'Altering [dbo].[PollingJBMS]...';


GO
ALTER PROCEDURE [dbo].[PollingJBMS]
	@Submitter varchar(50)
AS

	declare @JBMSPollingID int
	select	@JBMSPollingID = JBMSPollingID 
	from	JBMSPolling
	where	Submitter = @Submitter

	if @JBMSPollingID is null
	begin
		insert into JBMSPolling
			(Submitter, StatusID, LastUpdatedDateTime)
		values
			(@Submitter, 2, getdate())
		set @JBMSPollingID = @@IDENTITY
	end

	select	JBMSPollingID,
			Submitter,
			EntryID,
			SenderReference,
			MessageID,
			StatusID,
			LastUpdatedDateTime
	from	JBMSPolling
	where	JBMSPollingID = @JBMSPollingID

	RETURN @JBMSPollingID
GO
PRINT N'Creating [dbo].[SaveJBMSMessage]...';


GO
CREATE PROCEDURE [dbo].[SaveJBMSMessage]
	@MessageID VARCHAR(50), 
    @CustomerReference VARCHAR(MAX), 
    @DocumentID VARCHAR(50), 
    @Mailbox VARCHAR(MAX), 
    @MessageName VARCHAR(MAX), 
    @PartnerID VARCHAR(50),
    @StatusID INT,  
    @ErrorCount INT
AS
	INSERT into JBMSMessage
	(	
		MessageID, 
		CustomerReference, 
		DocumentID, 
		Mailbox, 
		MessageName, 
		PartnerID,
		ReceivedDateTime,
		StatusID, 
		LastUpdatedDateTime,
		ErrorCount
	)
	values
	(	
		@MessageID, 
		@CustomerReference, 
		@DocumentID, 
		@Mailbox, 
		@MessageName, 
		@PartnerID,
		getdate(),
		@StatusID,  
		getdate(),
		@ErrorCount
	)



RETURN 0
GO
PRINT N'Refreshing [dbo].[UpdatePolling]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[UpdatePolling]';


GO
PRINT N'Update complete.';


GO
